<UserControl x:Class="Особиста_Біблотека_Гнозіс.EditBookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="30">
        <StackPanel MaxWidth="500" HorizontalAlignment="Left">
            <TextBlock Text="📝 Редагування книги" FontSize="28" FontWeight="Bold" Margin="0,0,0,25" Foreground="#2C3E50"/>

            <TextBlock Text="Назва книги" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <TextBox x:Name="TitleInput" Padding="8" Margin="0,0,0,15"/>

            <TextBlock Text="Автор" Margin="0,0,0,5" FontWeight="SemiBold"/>
            <TextBox x:Name="AuthorInput" Padding="8" Margin="0,0,0,15"/>

            <TextBlock Text="Рік видання:"/>
            <TextBox x:Name="YearInput" 
             Margin="0,5,0,15" 
             Padding="5" 
             PreviewTextInput="NumberValidationTextBox"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>


                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Кількість сторінок" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <TextBox x:Name="PagesInput" Padding="8" Margin="0,0,0,15"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="Статус" Margin="0,0,0,5" FontWeight="SemiBold"/>
                    <ComboBox x:Name="StatusInput" Padding="8" Margin="0,0,0,15">
                        <ComboBoxItem Content="В наявності"/>
                        <ComboBoxItem Content="Віддана"/>
                        <ComboBoxItem Content="Замовлена"/>
                    </ComboBox>
                </StackPanel>
            </Grid>

            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                <Button Content="Зберегти" Click="SaveBtn_Click" Width="150" Height="40" 
                        Background="#4CAF50" Foreground="White" FontWeight="Bold" Margin="0,0,15,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Content="Скасувати" Click="CancelBtn_Click" Width="120" Height="40" 
                        Background="#9E9E9E" Foreground="White">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>