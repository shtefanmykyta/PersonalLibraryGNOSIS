<UserControl x:Class="Особиста_Біблотека_Гнозіс.AnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="📊" FontSize="32" Margin="0,0,15,0" VerticalAlignment="Center"/>
            <TextBlock Text="Аналітика бібліотеки" FontSize="28" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>
        </StackPanel>

        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,30">
            <Border Background="#E3F2FD" CornerRadius="10" Padding="20" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Загальний фонд" FontSize="14" Foreground="#1976D2"/>
                    <TextBlock x:Name="TotalBooksText" Text="0" FontSize="32" FontWeight="Bold" Foreground="#0D47A1"/>
                    <TextBlock Text="книг у базі" FontSize="12" Foreground="#1976D2" Opacity="0.7"/>
                </StackPanel>
            </Border>

            <Border Background="#F1F8E9" CornerRadius="10" Padding="20" Margin="5,0,5,0">
                <StackPanel>
                    <TextBlock Text="Об'єм знань" FontSize="14" Foreground="#388E3C"/>
                    <TextBlock x:Name="TotalPagesText" Text="0" FontSize="32" FontWeight="Bold" Foreground="#1B5E20"/>
                    <TextBlock Text="всього сторінок" FontSize="12" Foreground="#388E3C" Opacity="0.7"/>
                </StackPanel>
            </Border>

            <Border Background="#FFF3E0" CornerRadius="10" Padding="20" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="Автори" FontSize="14" Foreground="#F57C00"/>
                    <TextBlock x:Name="UniqueAuthorsText" Text="0" FontSize="32" FontWeight="Bold" Foreground="#E65100"/>
                    <TextBlock Text="унікальних імен" FontSize="12" Foreground="#F57C00" Opacity="0.7"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="#EEEEEE" BorderThickness="1" CornerRadius="10" Margin="0,0,15,0" Padding="20">
                <DockPanel>
                    <TextBlock Text="Розподіл за жанрами" DockPanel.Dock="Top" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="GenreList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="0,0,0,15">
                                        <Grid>
                                            <TextBlock Text="{Binding GenreName}" FontWeight="Medium" FontSize="14"/>
                                            <TextBlock Text="{Binding CountText}" HorizontalAlignment="Right" Foreground="Gray"/>
                                        </Grid>
                                        <ProgressBar Value="{Binding Count}" Maximum="{Binding TotalBooks}" 
                                                     Height="8" Foreground="#673AB7" Background="#F0F0F0" Margin="0,5,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Background="#F5F5F5" CornerRadius="10" Padding="20">
                <StackPanel>
                    <TextBlock Text="Стан фонду" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <TextBlock Text="🏠 В наявності" Margin="0,0,0,5"/>
                    <ProgressBar x:Name="HomeProgress" Height="15" Foreground="#4CAF50" Margin="0,0,0,15"/>
                    <TextBlock Text="🤝 Віддано" Margin="0,0,0,5"/>
                    <ProgressBar x:Name="BorrowedProgress" Height="15" Foreground="#2196F3" Margin="0,0,0,15"/>
                    <TextBlock Text="📦 Замовлено" Margin="0,0,0,5"/>
                    <ProgressBar x:Name="OrderedProgress" Height="15" Foreground="#FF9800"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>